import React from 'react'

export default function Footer(): React.JSX.Element {
  return (
    <footer className={'h-[300px] bg-base-200 flex justify-center items-center snap-start'}>
      contact
    </footer>
  )
}
